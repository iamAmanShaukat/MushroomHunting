<?xml version="1.0" encoding="utf-8"?>
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="?attr/colorControlHighlight"> <!-- Ripple color based on the theme highlight -->

    <!-- Background for normal state -->
    <item>
        <shape android:shape="rectangle">
            <corners android:radius="20dp"/>
            <stroke android:color="#FFF2EC" android:width="1dp"/>
            <solid android:color="#807674"/> <!-- Set the default background color here -->
        </shape>
    </item>

    <!-- Ripple mask -->
    <item android:id="@android:id/mask">
        <shape android:shape="rectangle">
            <corners android:radius="20dp"/>
            <solid android:color="#FFF2EC"/> <!-- Ripple mask color -->
        </shape>
    </item>

</ripple>